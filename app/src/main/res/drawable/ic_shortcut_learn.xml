<!-- The mortarboard, which is the skills icon. The site's own path from `next/components/icons.tsx`,
     stroked at the same weight on the same 24 grid. A shortcut
     icon is drawn white and the launcher tints it, which is why
     there is no accent here: the accent is the page's, and a
     shortcut has not opened one yet. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:strokeColor="#FFFFFF"
        android:strokeWidth="1.6"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M12,3l9,5 -9,5 -9,-5 9,-5zM5,11v5c0,1.7 3.1,3 7,3s7,-1.3 7,-3v-5" />
</vector>
